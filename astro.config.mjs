// @ts-check
import { defineConfig } from 'astro/config';

// O campo `site` é usado para gerar URLs canónicos e o sitemap.
// Aponta para o domínio final do blog.
export default defineConfig({
  site: 'https://areiadotempo.pt',
});
